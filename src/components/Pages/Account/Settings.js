import React from 'react'
import { Text, View } from 'react-native'

export const Settings = () => {
  return (
    <View className="settings-main min-h-screen flex justify-center items-center">
      <Text className="text-xl text-red-500">
        Hello from settings screen
      </Text>
    </View>
  )
}
